📘 SAGAU — Frontend & Backend Integration Documentation
📌 Project Name

Sagau — Freelance + Local Skilled Worker Marketplace

🧠 1. Overview

Sagau is a full-stack marketplace platform connecting:

👨‍💼 Clients (who post jobs)
🧑‍🔧 Workers (who apply for jobs)
🛡️ Admin (platform management)
Architecture:
Frontend (React + Vite)
↓
Axios API Layer
↓
Backend (Node.js + Express)
↓
MongoDB Database
🌐 2. Backend Base URL
Development
http://localhost:5000/api
Production
https://your-domain.com/api
🔐 3. Authentication Flow
3.1 Register User
Endpoint:
POST /api/auth/register
Request Body:
{
"fullName": "John Doe",
"email": "john@example.com",
"password": "123456",
"role": "client"
}
Response:
{
"success": true,
"message": "User registered successfully",
"data": {
"token": "JWT_TOKEN"
}
}
3.2 Login User
POST /api/auth/login
Request:
{
"email": "john@example.com",
"password": "123456"
}
Response:
{
"success": true,
"data": {
"token": "JWT_TOKEN"
}
}
3.3 Get Current User
GET /api/auth/me
Headers:
Authorization: Bearer JWT_TOKEN
🧭 4. Frontend Authentication Flow
Step Flow:
Login/Register → Receive JWT → Store in localStorage → Attach to Axios → Access Protected Routes
4.1 Token Storage (Frontend)
localStorage.setItem("token", token);
4.2 Axios Auto Token Injection
api.interceptors.request.use((config) => {
const token = localStorage.getItem("token");

if (token) {
config.headers.Authorization = `Bearer ${token}`;
}

return config;
});
🧑‍💼 5. Client APIs
Create Profile
POST /api/clients/create-profile
Get Profile
GET /api/clients/profile
Get Client Jobs
GET /api/clients/jobs
🧑‍🔧 6. Worker APIs
Create Profile
POST /api/workers/create-profile
Get Worker Profile
GET /api/workers/profile
Get All Workers
GET /api/workers
Get Worker Details
GET /api/workers/:id
💼 7. Job APIs
Create Job
POST /api/jobs
Get All Jobs
GET /api/jobs
Search Jobs
GET /api/jobs/search
Get Job Details
GET /api/jobs/:jobId
Select Worker
POST /api/jobs/:jobId/select-worker/:workerId
📩 8. Application System
Apply to Job
POST /api/applications/:jobId
Get My Applications
GET /api/applications/my-applications
Get Job Applications
GET /api/applications/job/:jobId
Update Application Status
PATCH /api/applications/:applicationId
🤝 9. Agreement System
Get Agreements
GET /api/agreements
Get Single Agreement
GET /api/agreements/:agreementId
Update Status
PATCH /api/agreements/:agreementId/status
⭐ 10. Review System (Frontend Integration)
Create Review
POST /api/reviews/:agreementId
Body:
{
"rating": 5,
"comment": "Excellent work"
}
Get Worker Reviews
GET /api/reviews/worker/:workerId
Get Client Reviews
GET /api/reviews/client/:clientId
🧩 11. Frontend API Layer Structure
src/api/
└── axios.js
Example API call:
import api from "../api/axios";

export const getWorkers = () => api.get("/workers");

export const loginUser = (data) =>
api.post("/auth/login", data);
🧠 12. Frontend State Management (Zustand)
src/store/authStore.js
Example:
const useAuthStore = create((set) => ({
user: null,
token: null,

setUser: (user) => set({ user }),

setToken: (token) => set({ token }),

logout: () => set({ user: null, token: null }),
}));
🔐 13. Protected Route Flow
User Login → Token Saved → Route Guard Checks Token → Access Granted
Logic:
if (!token) {
redirect("/login");
}
🎯 14. UI ↔ API Mapping
Worker Page
UI Component → GET /api/workers
Worker Profile Page
UI → GET /api/workers/:id + GET /api/reviews/worker/:id
Job Page
UI → GET /api/jobs
Job Details
UI → GET /api/jobs/:id + GET /api/applications/job/:id
Review Section
UI → POST /api/reviews/:agreementId
🚀 15. Data Flow Architecture
USER ACTION
↓
React Component
↓
Axios API Call
↓
Express Route
↓
Controller Logic
↓
MongoDB
↓
Response
↓
Frontend UI Update
⚠️ 16. Common Integration Rules
MUST FOLLOW:
Always use JWT in headers
Always use /api prefix
Never call DB directly from frontend
Validate backend responses
Use async/await for API calls
📌 17. Environment Variables
Frontend (.env)
VITE_API_BASE_URL=http://localhost:5000/api
Backend (.env)
MONGO_URI=your_mongo_url
JWT_SECRET=your_secret
PORT=5000
🧪 18. Testing Flow
Step-by-step:

1. Register user
2. Login user
3. Create profile (client/worker)
4. Create job
5. Worker applies
6. Client selects worker
7. Agreement created
8. Review submitted
   🏁 FINAL SUMMARY

Sagau frontend-backend system is built on:

REST API architecture
JWT authentication
Role-based access
Modular MVC backend
Feature-based React frontend
Axios communication layer
Zustand state management
🚀 Next Phase Recommendation

After this integration:

👉 Build Frontend in order:

Auth UI (Login/Register)
Worker Listing Page
Job Listing Page
Job Details Page
Application System UI
Agreement UI
Review System UI (final phase)

# 9. Frontend & Backend Integration Documentation

## Project Name

**Sagau — Freelance & Local Skilled Worker Marketplace**

---

# 1. Overview

Sagau is a full-stack marketplace platform designed to connect clients with skilled workers through a secure hiring and reputation-based ecosystem.

The system consists of:

### Client

Posts jobs and hires workers.

### Worker

Applies for jobs and completes assigned work.

### Admin

Manages users, verification, moderation, and platform operations.

---

# System Architecture

```text
Frontend (React + Vite)
          ↓
     Axios Layer
          ↓
Backend REST APIs
(Node.js + Express)
          ↓
      MongoDB
          ↓
 Cloudinary / AWS S3
```

---

# 2. Backend Base URL

## Development

```text
http://localhost:5000/api
```

## Production

```text
https://your-domain.com/api
```

---

# 3. Authentication Integration

## User Registration

### Endpoint

```http
POST /api/auth/register
```

### Request

```json
{
  "fullName": "John Doe",
  "email": "john@example.com",
  "password": "123456",
  "role": "client"
}
```

### Response

```json
{
  "success": true,
  "message": "User registered successfully",
  "data": {
    "token": "JWT_TOKEN"
  }
}
```

---

## User Login

### Endpoint

```http
POST /api/auth/login
```

### Request

```json
{
  "email": "john@example.com",
  "password": "123456"
}
```

### Response

```json
{
  "success": true,
  "data": {
    "token": "JWT_TOKEN"
  }
}
```

---

## Current User

### Endpoint

```http
GET /api/auth/me
```

### Headers

```http
Authorization: Bearer JWT_TOKEN
```

---

# 4. Frontend Authentication Flow

```text
Register/Login
       ↓
Receive JWT
       ↓
Save Token
       ↓
Axios Interceptor
       ↓
Protected Requests
       ↓
Authorized Access
```

---

## Token Storage

```javascript
localStorage.setItem("token", token);
```

---

## Axios Interceptor

```javascript
api.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");

  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }

  return config;
});
```

---

# 5. Frontend Folder Structure

```text
src/
│
├── api/
│   ├── axios.js
│   ├── authApi.js
│   ├── jobApi.js
│   ├── workerApi.js
│   ├── clientApi.js
│   ├── reviewApi.js
│   └── agreementApi.js
│
├── components/
│
├── pages/
│   ├── auth/
│   ├── jobs/
│   ├── workers/
│   ├── agreements/
│   └── reviews/
│
├── layouts/
│
├── routes/
│
├── store/
│   ├── authStore.js
│   ├── jobStore.js
│   └── workerStore.js
│
├── hooks/
│
├── services/
│
├── utils/
│
└── App.jsx
```

---

# 6. Client APIs

## Create Profile

```http
POST /api/clients/create-profile
```

## Get Profile

```http
GET /api/clients/profile
```

## Client Jobs

```http
GET /api/clients/jobs
```

---

# 7. Worker APIs

## Create Worker Profile

```http
POST /api/workers/create-profile
```

## Get Profile

```http
GET /api/workers/profile
```

## Worker Listing

```http
GET /api/workers
```

## Worker Details

```http
GET /api/workers/:id
```

---

# 8. Job APIs

## Create Job

```http
POST /api/jobs
```

## Get Jobs

```http
GET /api/jobs
```

## Search Jobs

```http
GET /api/jobs/search
```

## Job Details

```http
GET /api/jobs/:jobId
```

## Select Worker

```http
POST /api/jobs/:jobId/select-worker/:workerId
```

---

# 9. Application APIs

## Apply For Job

```http
POST /api/applications/:jobId
```

## My Applications

```http
GET /api/applications/my-applications
```

## Job Applications

```http
GET /api/applications/job/:jobId
```

## Update Application

```http
PATCH /api/applications/:applicationId
```

---

# 10. Agreement APIs

## List Agreements

```http
GET /api/agreements
```

## Single Agreement

```http
GET /api/agreements/:agreementId
```

## Update Status

```http
PATCH /api/agreements/:agreementId/status
```

---

# 11. Review APIs

## Create Review

```http
POST /api/reviews/:agreementId
```

### Request

```json
{
  "rating": 5,
  "comment": "Excellent work"
}
```

---

## Worker Reviews

```http
GET /api/reviews/worker/:workerId
```

---

## Client Reviews

```http
GET /api/reviews/client/:clientId
```

---

# 12. State Management (Zustand)

## Purpose

Centralized management of:

- User State
- Authentication State
- Job State
- Worker State
- UI State

---

## Example Store

```javascript
const useAuthStore = create((set) => ({
  user: null,
  token: null,

  setUser: (user) => set({ user }),

  setToken: (token) => set({ token }),

  logout: () =>
    set({
      user: null,
      token: null,
    }),
}));
```

---

# 13. Protected Route System

## Workflow

```text
Login
  ↓
Token Stored
  ↓
Route Guard
  ↓
Access Granted
```

---

## Example

```javascript
if (!token) {
  navigate("/login");
}
```

---

# 14. UI ↔ API Mapping

## Worker Listing Page

```text
WorkersPage
        ↓
GET /api/workers
```

---

## Worker Profile Page

```text
WorkerProfilePage
        ↓
GET /api/workers/:id
        +
GET /api/reviews/worker/:id
```

---

## Job Listing Page

```text
JobsPage
       ↓
GET /api/jobs
```

---

## Job Details Page

```text
JobDetailsPage
        ↓
GET /api/jobs/:id
        +
GET /api/applications/job/:id
```

---

## Review Component

```text
ReviewForm
      ↓
POST /api/reviews/:agreementId
```

---

# 15. End-to-End Data Flow

```text
User Action
      ↓
React Component
      ↓
Axios API Call
      ↓
Express Route
      ↓
Controller
      ↓
Service Layer
      ↓
MongoDB
      ↓
Response
      ↓
State Update
      ↓
UI Re-render
```

---

# 16. Integration Standards

## Mandatory Rules

### Authentication

- Always attach JWT token.

### API Structure

- Always use `/api` prefix.

### Database Access

- Frontend never communicates directly with MongoDB.

### Error Handling

- Validate every backend response.

### Async Processing

- Use async/await for all API calls.

---

# 17. Environment Variables

## Frontend (.env)

```env
VITE_API_BASE_URL=http://localhost:5000/api
```

---

## Backend (.env)

```env
MONGO_URI=your_mongodb_connection
JWT_SECRET=your_secret_key
PORT=5000
```

---

# 18. Testing Workflow

```text
1. Register User
          ↓
2. Login
          ↓
3. Create Profile
          ↓
4. Create Job
          ↓
5. Apply To Job
          ↓
6. Select Worker
          ↓
7. Create Agreement
          ↓
8. Complete Work
          ↓
9. Submit Review
```

---

# 19. Frontend Development Order

## Phase 1

- Login Page
- Register Page
- Authentication Store
- Protected Routes

---

## Phase 2

- Worker Listing
- Worker Profile
- Search System

---

## Phase 3

- Job Listing
- Job Details
- Job Creation

---

## Phase 4

- Application System
- Hiring Workflow

---

## Phase 5

- Agreement Management
- Work Tracking

---

## Phase 6

- Reviews & Ratings
- Reputation Display

---

# Final Summary

Sagau uses a modern full-stack architecture based on:

- React + Vite Frontend
- Axios Communication Layer
- Zustand State Management
- Node.js + Express Backend
- MongoDB Database
- JWT Authentication
- REST API Architecture
- Role-Based Access Control (RBAC)

This integration architecture provides a scalable foundation for a marketplace platform capable of supporting job posting, worker discovery, hiring workflows, agreement management, and reputation building.
